# 「小计划」借鉴习惯打卡模板 — 可行性分析

> 分析日期：2026-08-31
> 分析范围：只读，**未修改任何代码**
> 团队：架构师 / 设计师 / PM 三方并行分析 + Team Lead 代码复核

---

## 0. 一句话结论

**能借鉴，但只能借 30%。而且必须先补同步短板，再谈视觉。**

模板是**个人 × 自我量化 × 成就驱动**；小计划是**双人 × 协作 × 低压力**。
重合的只有「习惯打卡」一个点。模板最亮眼的四件套里，心情曲线、个人徽章、自由取色器在双人语境下会**反向伤害**产品。

更重要的：本次分析顺带定位了 **3 个已存在的 P0 缺陷**，它们比"好不好看"紧急得多。

---

## 1. 结构差异：模板 4 页 ≠ 小计划 5 Tab

模板的四个页面在小计划里的对应情况：

| 模板页面 | 小计划现状 | 性质 |
|---|---|---|
| 今日打卡（进度环+连续天数+心情） | 今日 = **任务清单**（四态+收尾） | 不同性质的东西，进度环可以嫁接 |
| 习惯管理 | 有，但只有布尔打卡 | 唯一真正重合的点 |
| 数据统计 | **完全不存在** | 空白，需新建 |
| 个人设置 | **不存在**（"更多"其实是 特别日子/第一句话/悄悄话/共享清单） | 空白，需新建 |

结论：这不是"换个皮"，是 **补两块新能力 + 换一层皮**。

---

## 2. ⚠️ 顺带发现的既有 P0 缺陷（比借鉴本身更紧急）

这部分是本次分析的**意外收获**，均已由 Team Lead 直接读码验证。

### P0-1 删除同步无云端 tombstone

**现象**：用户反馈"删除倒是能同步了，但是有时候会出问题"。

**验证过程**：
- `mergeRows` 的删除传播**其实是正常的** — `index.html:539` 有 `if(!c) continue; // 云端已删 → 本机跟着删`，设备 B 拉取时本地行不在云端就丢弃。
- **真正的问题在别处**：`S.pendingDeletes`（`index.html:458` 入队、`512` 落盘）**只活在 localStorage**，云端没有任何软删除标记。

**失效场景**：
1. 离线时删除 → 队列还在本地 → 清缓存 / 换设备 → 队列丢失 → 记录**在云端永久复活**
2. `delete()` 持续失败时（`index.html:509`）只 `console.warn`，**无 toast**，用户以为删成功了

**最小改法**：给 5 张计划表加 `deleted_at timestamptz`，删除改走软删 + 查询过滤。

### P0-2 `save()` 无异常保护，localStorage 撑爆会中断同步

- `pullPlans`（`index.html:550-556`）**无时间窗全量拉取**历史数据
- `save()`（`index.html:277`）**没有 try/catch**
- 配额溢出 → 抛异常 → 同步流程直接中断，且用户无感知

### P0-3 习惯整行 LWW，多设备必然丢打卡

- `mergeRows` 是**整行替换**（`index.html:540`），不是字段级合并
- `habits.done_dates` 是整个 `date[]` 数组作为一个字段
- **场景**：手机打卡了 8/28，电脑打卡了 8/29（各自离线）→ 后写的整行覆盖 → **先写的那条打卡消失**

> 这是用户"只同步了添加计划"之后会遇到的下一颗雷，目前还没爆是因为他没在多设备同时打同一个习惯。

### P1 月目标 `done` 是死字段（确认）

`bindMonth()`（`index.html:940`）写入时带 `done:false`，但：
- `toCloud('monthGoals')`（`index.html:419`）不传
- `fromCloud`（`index.html:431`）不读
- `schema.sql` 的 `month_goals` 表**没有这一列**

后果：月目标永远无法标记完成，且代码里有一个永不生效的字段。

### P1 无障碍 / 触控（设计师实测）

| 位置 | 问题 |
|---|---|
| `index.html:82` | `button{color:#fff}` 在 `--accent:#c2774f` 上对比度仅 **≈3.6:1**，14px 正文不达 WCAG AA（需 4.5:1） |
| `index.html:1038/1044` | 共享清单 `.check` 被内联改成 **22×22px**，低于 44×44 触控标准，且是最高频操作 |
| `index.html:92` `.iconbtn` | 34px，低于标准 |
| `index.html:89` `button.sm` | ≈34px，低于标准 |
| `index.html:5` | `maximum-scale=1` 禁缩放，违反 WCAG 1.4.4 |
| `index.html:116` | `backdrop-filter: blur(12px)` 是 iOS 移动端头号掉帧源 |
| 全文件 | **0 处 `:focus-visible` 焦点环** |

---

## 3. 借鉴清单：分三档

### 🟢 直接抄（纯视觉层，零数据风险，约 0.5 天）

| # | 条目 | 说明 |
|---|---|---|
| 1 | 波点背景 | 3 行 CSS `radial-gradient` 平铺，不碰 DOM 与数据 |
| 2 | 派生色阶 | **前提**：先把 76/100/129/144/156/180/187 行的硬编码色收进 token，否则必漏改 |
| 3 | 文字颜色自动适配 | 顺手修掉 P1 的白字 3.6:1 缺陷 |
| 4 | 圆角/间距 token 化 | 现有 12/16/20/10/999 五档并存，无 token |
| 5 | 补齐 hover / focus-visible / disabled / loading 四态 | 当前只有 `:active{opacity:.75}` |
| 6 | 删掉 `maximum-scale=1` | 一行 |
| 7 | 扩大触控命中区 | 见上表，用 `padding+负 margin` 或 `::after` 扩区，不撑布局 |
| 8 | 桌面响应式 | `max-width:720px` 居中 + 双栏（只给"今日""更多"开）+ `.sheet` 改居中弹窗 |

**色彩技术方案**（设计师给出，零依赖可行）：
- 主方案 **JS 计算 HSL/RGB 混合后写 CSS 变量**；`color-mix()` 仅作渐进增强
- 理由：`color-mix()` 需 iOS Safari 16.2+，加到主屏的老设备不自动升级；且它只能做一步线性混合，**做不了"多步混合 + 亮度判定"**
- 文字对比度**不能用"亮度 >0.5 取黑"**（`#c2774f` 相对亮度 ≈0.20 会被误判），必须用**对比度择优**：正文 ≥4.5:1，大字 ≥3:1

```js
// 派生色彩核心（示意，未落地）
const mix = (a,b,t) => a.map((v,i)=>v+(b[i]-v)*t);
const lum = a => { const f=a.map(v=>{v/=255; return v<=0.03928?v/12.92:((v+0.055)/1.055)**2.4;});
                   return .2126*f[0]+.7152*f[1]+.0722*f[2]; };
const contrast=(a,b)=>{const[x,y]=[lum(a),lum(b)].sort((m,n)=>n-m);return (x+.05)/(y+.05);};
r.setProperty('--card',       rgb2hex(mix(B,[255,255,255],.55))); // 卡片提亮
r.setProperty('--card-inset', rgb2hex(mix(B,A,.10)));             // 内嵌方块掺 10% 强调色
r.setProperty('--accent-soft',rgb2hex(mix(B,A,.22)));
```

### 🟡 改造后抄（有价值，但要动结构 / 需先修 P0）

| # | 条目 | 成本 | 依赖 |
|---|---|---|---|
| 9 | 今日进度环 + 连续收尾天数 | 低 | 无，可立即做 |
| 10 | 30 天热力图 | 中 | 数据现成（`S.habits[].doneDates`），但要先决定"画谁的" |
| 11 | 周趋势柱图 | 低（~30 行） | 同上 |
| 12 | 习惯**计数**模式（如喝水 8 杯） | 中 | 需 `habit_logs` 表，见 §4 |
| 13 | 桌面双栏布局 | 低 | 见 🟢-8 |

### 🔴 不建议抄（与产品定位冲突）

| # | 条目 | 否决理由 |
|---|---|---|
| 14 | **自由取色器**（用户自选主题背景+强调色） | ① 双人共用，各自设各的主题 → 两人界面不一致 → **破坏"这是我们的 App"的共有感**；要一致就得新增云端共享设置表 + 冲突处理。② 登录页 `167-174` 的品牌识别已建立在暖米白+橙棕上。**替代方案：给 3-4 个预设主题（季节/节日），不给取色器。** |
| 15 | **心情曲线** | ① 无数据模型，需新增字段 + 每日录入 UI，比画图贵 3 倍。② **情侣场景里"每天给心情打分"且对方可见，会把陪伴工具变成审视工具** —— 催生表演性打卡（不敢填"差"）和猜疑。 |
| 16 | **个人成就徽章** | ① 需历史快照与规则引擎，而当前 `syncDelete`（`index.html:807`）删除即消失，数据基础不存在。② **双人场景里个人徽章就是排行榜**：我连续 30 天你只有 3 天，这不是激励是压力。③ "连续打卡 30 天！"的游戏化语气与现有文案（"没有延期的事，轻松。"/"今天已经收尾啦 · 明天见"）**直接冲突**。 |
| 17 | **时长 / 时间段打卡** | ① 在"每天打开 3 次、每次 30 秒"的工具里没人用。② `timestamptz` 是**历史事故高发区**（`done_time` 的 22008 错误刚修完）。③ 会把同步引擎的数组合并搞复杂。**建议 MVP 只做打勾 + 计数两种。** |

---

## 4. 技术方案要点（架构师）

### 4.1 为什么 `done_dates date[]` 装不下数值

`schema.sql:80` 的 `done_dates date[]` 只能表达"某天做了没"，装不下"做了几次/多少分钟/几点到几点"。

### 4.2 推荐方案：`habits` 加 `type`/`target` + 新建 `habit_logs`

| 维度 | 方案一（新表） | 方案二（编码进数组 `"2026-08-28:30"`） |
|---|---|---|
| 同步冲突 | ✅ 冲突域缩到「单习惯单天一个值」 | ❌ 冲突域是整个习惯对象，双设备必然丢数据 |
| 查询能力 | ✅ SQL 可聚合，能算周趋势 | ❌ 只能拉回前端解析 |
| 迁移成本 | 中（新建表 + 回填） | 低 |
| RLS 复杂度 | 中（多一张表的策略） | 低（无新增） |

**推荐方案一。** 决定性因素是同步冲突。

### 4.3 ⚠️ 关键设计决策：`habit_logs` 主键必须是**确定性 UUID v5**

这是一个容易踩的坑，架构师特别标出：

- 主键必须由 `(user_id, habit_id, day)` **哈希生成确定性 UUID**
- 若两台设备为同一天各自生成随机 id → 违反唯一约束 → 同步失败
- 若改用文本复合主键（如 `"h1_2026-08-28"`）→ **永远不匹配 `isUuid()` 闸（`index.html:409`）** → `flushPending` 永远走 insert 分支 → **重复键死循环**

> 这个约束和现有的 `isUuid()` 门禁强耦合，不能想当然。

### 4.4 统计：实时算，不要物化

数据量小（单人 10 个习惯 × 365 天 × 1 年 = 3650 行），JS 实时计算无压力。**但要配合 P0-2 一起修**（`pullPlans` 需要时间窗限制，否则历史全塞进 localStorage）。

### 4.5 迁移安全性：不破坏现有两个真实用户

纯增量迁移（只加列、只建新表、**保留 `done_dates`**），可随时回滚。

**⚠️ 迁移顺序不可颠倒：必须先跑 SQL，再部署前端。**

---

## 5. 建议路线图

### Phase 1 — 让同步闭嘴（可独立上线，风险低）
- 删除 tombstone（P0-1）
- 同步状态可见 UI
- 习惯差量合并（P0-3）
- 月目标可完成 / 目标可编辑（P1）
- 今日进度环
- **搭车**：视觉打磨小包（间距/字重/圆角/微动效/空状态 + 波点背景），0.5 天

> 收益：把用户前 5 轮抱怨一次性清零，重建信任。

### Phase 2 — 看见对方（风险中，涉及新 RLS 与隐私边界）
- 对方今日概览（**只读、粒度可控**）
- 双方收尾状态
- 共同徽章（非个人徽章）

> 收益：把"双人共用"从 30% 拉到 70%。**这是模板给不了的差异化，也是小计划唯一的护城河。**

### Phase 3 — 好看 + 回看（风险低）
- 30 天热力图
- 习惯计数模式
- 主题预设 3-4 个

---

## 6. 需要你拍板的 5 个问题

| # | 问题 | 团队推荐默认值 |
|---|---|---|
| 1 | 心情数据是否共享给伴侣？ | **个人私有，绝不自动共享。** 只做"主动贴给 ta"的一次性分享 |
| 2 | 热力图统计谁的？习惯还是任务？ | **个人习惯 + 个人任务各一张**；另加"我们"合并图，**默认关闭** |
| 3 | 主题色是否跨设备/跨人同步？ | **本机设置，不同步。** 两人界面一致才有"我们的"感觉 |
| 4 | 能看到对方的什么？ | **只看"今天完成/未完成"计数和任务标题**；**不公开偷懒次数和延期次数**（负面数据公开会让人羞耻，进而不敢用 App） |
| 5 | 徽章是个人还是共同？ | **只做共同徽章**，个人徽章在双人场景会变排行榜 |

---

## 7. 附：一个容易被忽略的设计气质冲突

设计师指出，现有 `index.html:977` 已经显示 **「连续 X 天」**——它**已经在制造断签焦虑**，与"偷懒也被接受"的设定（`index.html:762`「今天偷的懒」）**自相矛盾**。

建议：**只在 ≥7 天时才显示连续天数**，避免第一天没打卡就看到刺眼的"连续 0 天"。

---

## 8. 附录：增量迁移方案已产出（草稿，未执行）

完整方案见 **`supabase/migration_p2_draft.sql`**（769 行）。

**文件头已标注「草稿方案，未经用户确认，不要执行」——一条语句都没跑过。**
已有文件全部未被改动（`schema.sql` 时间戳仍为 8/28，其余迁移文件为 8/29）。

### 8.1 ⚠️ 全案最脆的一环：确定性 UUID v5 —— 已独立验证通过

`habit_logs` 的主键必须由 `(user_id, habit_id, day)` 哈希得出。若两台设备各自生成随机 UUID → 撞唯一约束 → 同步失败；若用文本复合主键 → 永远过不了 `isUuid()` 门禁（`index.html:409`）→ `flushPending` 永远走 insert 分支 → **重复键死循环**。

所以必须是 uuid 格式，且前后端要算出**完全一致**的 id。

**验证方法与结果**（Team Lead 独立复核，不依赖子代理的自述）：

| 项目 | 结果 |
|---|---|
| 基准 | Python `uuid.uuid5`（RFC 4122 权威实现）生成期望值 |
| 被测 | 前端纯 JS 实现（`utf8Bytes` + `sha1Bytes` + `uuidV5`） |
| 用例数 | **12 / 12 全部通过** |
| 覆盖 | RFC 4122 标准向量、空串、SHA-1 分块边界（55/56/63/64/65 字节）、中文 UTF-8、真实业务形态、大写 UUID 输入 |
| `isUuid()` 门禁 | **通过** |

验证脚本：`_gen_uuid_expect.py`（生成期望值）+ `_verify_uuid_v5.js`（对拍）。

**技术选型理由**：用自写 `uuid_v5()` + pgcrypto `digest(x,'sha1')`，而不是 `md5()` 拼装（那是 UUID **v3** 不是 v5，MD5 已废弃且在 FIPS 模式下不可用），也不是 `uuid-ossp` 的 `uuid_generate_v5()`（算法等价，但要额外装扩展且是黑盒，前端得反推测试）。pgcrypto 已在 `schema.sql:7` 装好。

### 8.2 迁移的三个部分

| 部分 | 内容 | 关键设计 |
|---|---|---|
| §1 习惯打卡升级 | `habits` 加 `type`/`target`（CHECK 约束限定 `'check'\|'count'`）+ 新建 `habit_logs` | 只做**打勾 + 计数**，时长/时间段明确砍掉（`timestamptz` 是 22008 事故高发区）。取消打卡 = 写 `value=0` 而非物理删除，靠 `(habit_id, day)` 唯一约束天然保证一天一条 |
| §2 删除 tombstone | 5 张计划表加 `deleted_at` + 归档表 + 回收函数 | 删除改走软删；`purge_deleted()` 按外键依赖顺序删（`month_goals` 必须先于 `year_goals`），并加 `not exists` 守卫防止硬删年目标把活着的月目标外键抹成 null |
| §3 历史回填 | `done_dates` 展开 → `habit_logs` | 幂等（on conflict do nothing）；**`done_dates` 列全程不动**，随时可回滚 |

### 8.3 执行顺序（不可颠倒）

```
0. 备份（两个真实用户，不能省）
1. 跑 §0 自检 → 必须两行都「自检通过」，否则停
2. 前端先发「只读兼容版」（DB 还没动，零风险）
   ↑ 目的：让慢更新的设备先具备识别 deleted_at 的能力，
     否则一旦上 DDL，旧 bundle 会把软删的行 merge 回来（已删数据复活）
   同时 sw.js:2 的 CACHE 必须 v7 → v8
3. 跑 §1 + §2（顺序不能反，§3 回填用到 §2 的 deleted_at）
4. 跑 §4 验收查询：对得上=true、id_mismatch_rows=0
5. 跑 §3 回填
6. 前端发完整版
7. 观察 3~7 天再考虑挂 purge_deleted 定时任务
```

### 8.4 风险评估摘要

| # | 风险 | 等级 |
|---|---|---|
| R-1 | 会不会破坏现有两个真实用户的数据？ | **低** — 全是加列/加新表/加索引，无任何 `update`/`drop`/`alter column type`；`done_dates` 全程不动，§1 可 100% 回滚 |
| R-3 | 旧 bundle（SW 缓存）混合期 → 已删数据复活 | **中** — 靠阶段 2 先发只读兼容版 + SW 版本 +1 缓解 |
| R-4 | count 模式并发计数不精确（两台设备各 +1 应得 2，max 合并得 1） | **中** — 概率低；严格解法是 PN-counter，本次不做 |
| R-6 | 增量游标依赖客户端时钟，回拨会漏拉 | **中** — 5 分钟重叠吸收小漂移 |
| R-7 | `HABIT_NS` 命名空间常量一旦上线不能改 | **后果高 / 概率低** — 改 = 所有历史主键失配 |

### 8.5 一个反直觉的结论

子代理明确指出：**不要做「无脑只拉最近 90 天」的时间窗**——`special_dates` 的日期可能在明年、`year_goals` 可能是去年的，但它们的 `updated_at` 很久以前。按 `updated_at` 砍窗口会**静默丢数据**。

正确做法是**按表分流 + 冷/增量双模**：年/月/特别日子/习惯永远全量（行数量级很小），只有 `tasks` 和 `habit_logs` 需要窗口。

而且：两个真实用户累计也就几千行 tasks，全量拉一次几百 KB，**网络量根本不是当前的主要矛盾**。缺陷 3 真正的痛点在 `save()` 的 localStorage 配额。所以时间窗是「顺手做」，`save()` 保护才是「必须做」。

IndexedDB 建议**本次不做**，列为中期——换 IndexedDB 意味着 `loadLocal`/`save` 全变异步，`S` 的所有同步读写点都要改，改动面远大于本次迁移，不该塞进一个「修复缺陷」的增量包。

---

## 9. 本次分析的产物清单

| 文件 | 说明 |
|---|---|
| `FEASIBILITY-借鉴模板分析.md` | 本文档 |
| `supabase/migration_p2_draft.sql` | 增量迁移草稿（**未执行**） |
| `_gen_uuid_expect.py` / `_verify_uuid_v5.js` | UUID v5 前后端一致性验证脚本（12/12 通过） |
